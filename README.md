
## To build the project with a native image 

This will increment the version number and add it to give a new version to the client caller in initialize
```bash
./gradlew clean prebuild nativeCompile 
```

